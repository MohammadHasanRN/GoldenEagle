import React from 'react';

import { Text } from '@Components';
import { Box } from '@Theme';

export const MenuScreen = () => {
  return (
    <Box flex={1} alignItems="center" justifyContent="center">
        <Text variant="H4Semi">Menu Screen</Text>
    </Box>
  );
};